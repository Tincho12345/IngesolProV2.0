    @{
        Layout = null;
    }
    <!DOCTYPE html>
    <html lang="es">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Acceso Denegado - Efecto Linterna</title>
        <style>
            html, body {
                margin: 0;
                height: 100%;
                overflow: hidden;
                font-family: sans-serif;
                color: white;
                text-align: center;
            }

            .background {
                position: absolute;
                width: 100%;
                height: 100%;
                background: url('/NotAuthorize.avif') no-repeat center center;
                background-size: cover;
                filter: brightness(0.3);
            }

            .overlay {
                position: absolute;
                width: 100%;
                height: 100%;
                background: radial-gradient(circle at var(--x, 50%) var(--y, 50%), transparent 100px, rgba(0,0,0,0.6) 400px);
                pointer-events: none;
                transition: background 0.2s;
            }

            .content {
                position: relative;
                color: red;
                z-index: 2;
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                height: 100vh; /* altura completa de la ventana */
                text-align: center;
            }

            h1 {
                font-size: 4rem;
                margin-bottom: 1.5rem;
            }

            a {
                display: inline-block;
                margin-top: 20px;
                padding: 10px 20px;
                background: #ff4c4c;
                color: white;
                text-decoration: none;
                border-radius: 5px;
            }
        </style>
    </head>
    
    <body>
        <div class="background"></div>
        <div class="overlay" id="overlay"></div>

        <div class="content">
            <h1>Acceso Denegado</h1>
            <p>No tienes permiso para acceder a esta página.</p>
            <a href="/">Volver al inicio</a>
        </div>

        <script>
            const overlay = document.getElementById('overlay');

            document.addEventListener('mousemove', (e) => {
                const x = e.clientX / window.innerWidth * 100;
                const y = e.clientY / window.innerHeight * 100;
                overlay.style.setProperty('--x', `${x}%`);
                overlay.style.setProperty('--y', `${y}%`);
            });
        </script>
    </body>
    </html>
